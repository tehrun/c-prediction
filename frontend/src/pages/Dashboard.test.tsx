import {render,screen,waitFor} from '@testing-library/react'; import {vi,test,expect,beforeEach} from 'vitest'; import App from '../App';
beforeEach(()=>{vi.restoreAllMocks(); history.pushState(null,'','/')});
test('dashboard loading and empty state',async()=>{vi.spyOn(globalThis,'fetch').mockImplementation((async (u:RequestInfo|URL)=>{const path=String(u); if(path.includes('/market/universe')) return new Response('[]'); if(path.includes('/market/data-health')) return Response.json({status:'unavailable',latest_candle:null,stale_markets:0,message:'No data'}); return Response.json({status:'healthy',details:{database:'healthy'}})}) as typeof fetch); render(<App/>); expect(screen.getByText(/Loading dashboard/)).toBeInTheDocument(); await screen.findByText(/No market data yet/);});
test('universe rendering',async()=>{vi.spyOn(globalThis,'fetch').mockResolvedValueOnce(Response.json({status:'healthy',details:{database:'healthy'}})).mockResolvedValueOnce(Response.json({status:'healthy',details:{}})).mockResolvedValueOnce(Response.json({status:'healthy',latest_candle:null,stale_markets:0,message:null})).mockResolvedValueOnce(Response.json([{exchange:'kraken',symbol:'BTC/USD',base_currency:'BTC',quote_currency:'USD',active:true}])); render(<App/>); await screen.findByText('BTC/USD');});
test('api error state',async()=>{vi.spyOn(globalThis,'fetch').mockResolvedValue(new Response('',{status:500})); render(<App/>); await waitFor(()=>expect(screen.getByRole('alert')).toHaveTextContent('API error'));});
